{
  "name": "kaji-quest-line",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Sheets → HTML → JPEG → docs/ → GitHub Pages → LINE",
  "scripts": {
    "build": "node scripts/build.mjs",
    "send-line": "node scripts/send-line.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "googleapis": "^144.0.0",
    "playwright": "^1.49.1",
    "sharp": "^0.33.5"
  }
}

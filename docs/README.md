# GitHub Pages（`/docs`）

このフォルダは **GitHub Pages** の公開ルートです。`today.jpg` / `today_preview.jpg` は GitHub Actions が毎朝生成してコミットします。

リポジトリ設定: **Settings → Pages → Build and deployment → Source: Deploy from a branch → Branch `main` / folder `docs`**。
